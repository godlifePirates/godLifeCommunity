<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.glp.community.mapper.BoardMapper">
    <select id="getBoardList" resultType="com.glp.community.data.dto.BoardDTO">
        SELECT
            seq as seq,
            name as name
        FROM tbl_board
    </select>
</mapper>